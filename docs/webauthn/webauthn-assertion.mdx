---
sidebar_position: 2
slug: /webauthn/assertion
id: webauthn-assertion
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import BrowserWindow from "@site/src/components/BrowserWindow";

# Device Assertion

WebAuthN authenticators use cryptographic keys to perform an operation called assertion 

Whenever a user registers a device as a WebAuthN authenticator with a new service a unique key pair, called the "device key pair" is generated. The publi

Another key pair is also used, 

